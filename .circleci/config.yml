version: 2.1
jobs:
  build:
    docker: 
      - image: zephyrprojectrtos/ci:v0.10.1
    steps:
      - checkout:
           path: zephyr
      - run:
          command: |
            pwd
            ls

